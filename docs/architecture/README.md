# Architecture

This folder describes the current intended architecture of `tomorrow_pitcher`.

Use decisions for "why we chose this." Use architecture docs for "what the current system looks like."

Core docs:

- `system-overview.md`: product/runtime architecture and data flow.
- `environments.md`: local, production, secrets, and deployment environments.

