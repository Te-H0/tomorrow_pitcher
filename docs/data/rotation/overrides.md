# 로테이션 수동 보정 (overrides)

> 사람 판단의 **단일 진입점**. 자동 파생(slots.md)이 틀렸을 때만 행을 추가한다.
> `처리`=제외: 파생 풀에서 뺀다(부상·보직 변경 등). `처리`=포함: 풀에 강제 추가한다(복귀 예정 등, `기준일`=마지막 등판일로 취급할 YMD, 비우면 5일 전).
> 해소되면 행을 지운다. 뉴스 근거가 있으면 메모에 링크.

| 팀 | 투수 | 처리 | 기준일 | 메모 |
|---|---|---|---|---|
