# 2026-07-18 로테이션 시드 검토 + 예고 파서 오염 수정

## 한 일

- 자동 시드(07.08~17 기반, 올스타 브레이크로 표본 부족)를 **브레이크 이후 시퀀스(07.16~18 실제 + 07.19 공식 예고) 기준으로 재구성**. 팀별 슬롯 1~4는 검증된 순서, 슬롯 5는 6월 멤버 기반 미검증 후보(`TEMPORARY`).
- 포인터를 07.21(화) 예상 슬롯으로 수동 설정(`manual`). 07.21 predicted 행은 경기 3일 전 관리자 수동 보정으로 갱신(`슬롯 N (수동 보정)` 표기). 07.18·19 예측은 동결 유지 — 잘못된 시드로 한 예측도 검증 기록.
- **예고 파서 라이브 오염 버그 수정**: 경기 시작 후에는 `away_p_id/home_p_id`가 예고 선발이 아니라 현재 투수로 바뀜(07.18 실측 — 세베리노 등 불펜 투수가 예고로 기록됨). `game_sc=1`(경기 전)일 때만 수집하도록 필터 추가, 오염된 07.18 행 5개 + 변경 이력 3행 제거.
- 첫 예측 성적: 07.18 — 잠정 시드로 10팀 중 5팀 적중 (네일·임찬규·나균안·구창모·잭로그).

## 실측으로 확인된 정책 사례

- **우천취소 이월 2형태**: 롯데는 07.17 예고(나균안) 취소 → 07.18 그대로 이월. 삼성은 07.17 예고(원태인) 취소 → 07.18 페덱 투입, 원태인 07.19로 재배치. → "취소 = 단순 이월" 가정 금지, 운영자 판단 영역이라는 기존 결정을 실데이터가 뒷받침.

## 본개발 반영 사항 (발견)

1. `SYSTEM_PREDICTED` 수동 보정은 경기 전이면 허용되어야 함(관리자 페이지 핵심 기능). 예측 불변 규칙은 "경기 후 사후 수정 금지"로 좁혀 정의할 것. 보정 이력 필드 필요.
2. 포인터-자동전진 의미 충돌: 포인터가 미래 경기(D+2~3)를 가리키면 "어제 실제와 비교" 로직이 항상 불일치로 auto-hold 노이즈를 냄. DB 버전에서는 `last_advanced_game_id`로 경기 단위 추적하여 해소 (스키마 노트에 이미 예정된 설계).
3. GameCenter `p_id` 속성 신뢰 조건: `game_sc=1`에서만 예고로 신뢰 — DB 임포터에도 동일 규칙 적용.
